## 2026-03-29

### 应用名称修改
- 将应用显示名称从 TrollVNC 改为 MatisuXCS
- 修改了 Xcode 项目配置和 InfoPlist.strings 文件

### 修复智能清理、重启、注销功能
**问题**: 用户反馈这些功能无效

**改进内容**:
1. **重启设备 (rebootDevice)**:
   - 添加多种重启方法（notify_post、posix_spawn）
   - 添加详细调试日志
   - 添加 HID 电源键作为备选方案

2. **注销设备 (respringDevice)**:
   - 添加多种 respring 通知尝试
   - 添加 killall SpringBoard 和 backboardd
   - 添加详细调试日志

3. **智能清理 (clearBackgroundAppsSmart)**:
   - 添加详细调试日志诊断问题
   - 改进桌面检测逻辑
   - 添加更多系统应用白名单
   - 检查 HID 生成器是否可用

**待验证**: 需要在设备上测试查看日志输出，确定具体失败原因

### 针对 iOS 15 进一步优化
**用户系统**: iOS 15

**改进内容**:
1. **重启设备**:
   - 优先使用 `reboot()` 系统调用（添加 `sys/reboot.h`）
   - 备选 notify_post 和 posix_spawn

2. **注销设备**:
   - 优先使用 `killall SpringBoard`
   - 备选 `killall backboardd`
   - 最后尝试 notify_post

3. **智能清理**:
   - 改进桌面检测，处理 `SBSCopyFrontmostApplicationDisplayIdentifier` 返回 nil 的情况
   - 通过应用状态辅助判断
