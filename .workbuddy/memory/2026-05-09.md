# 2026-05-09

## TrollVNC-main 设置页按钮/设备信息修复

- **Root.plist action 冒号缺失**：注销(`logoutDevice`)和重启(`restartDevice`)按钮的 action 少了冒号，导致 Preferences 框架找不到对应 selector，改成 `logoutDevice:` 和 `restartDevice:`
- **PSTitleValueCell 显示为空**：`TVNCRootListController.m` 里用 `readPreferenceValue:` 读 NSUserDefaults，但设备信息用的是 `get` 属性绑定 getter 方法，导致返回 nil。修复：改用 `methodForSelector` 直接调用 getter 方法
