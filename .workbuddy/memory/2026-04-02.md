# 2026-04-02

## TrollVNC respring 优化
- 使用 sysctl + proc_pidpath + kill 直接终止进程（无需 spawn 新进程）
- respringDevice 只杀三个核心进程：SpringBoard、FrontBoard、BackBoard
- 移除了 posix_spawn、popen、notify_post 等备用方法
- 添加 libproc.h 头文件，malloc 强制转换解决 C++ 兼容性

## TrollVNC API 精简优化 (commit a9cbd4e)
- respring 注销后等待解锁时间：30s → 15s
- /api/screen/lock 锁屏：改用 powerPress 模拟按电源键（替代 hardwareLock）
- /api/clearapps/smart 修复：改为直接按 Home 键退出前台应用；通过 getFrontmostAppBundleID 判断是否在桌面
- 删除 /api/clearapps 端点（含路由、Handler、ApiManager 方法）
- 删除所有 AssistiveTouch 相关 API：状态/启用/禁用/锁定/解锁（共5个端点）
- 同步清理 TVNCApiManager.h 声明、test_api.html、API_DOCUMENTATION.md

